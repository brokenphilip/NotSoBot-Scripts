# Help v1.0

print("## Commands:")
print("1. :pushpin: `{}t run help` - Shows a list of all available commands".format(nsb_prefix))
print("2. :pushpin: `{}t run info` - Returns all the information currently accessible through the main tag".format(nsb_prefix))
print("3. `{}t run fm [count]` - Gets the dates and times for the next `count` Full Moon holidays in Team Fortress 2".format(nsb_prefix))
print("4. `{}t run gw2 [count]` - Gets the dates and times for the next `count` cycles/resets of (star) quests in Plants vs Zombies: Garden Warfare 2".format(nsb_prefix))
print("5. `{}t run th [count]` - Gets the names, dates and times for the next `count` Town Hall events in Plants vs Zombies: Garden Warfare 2".format(nsb_prefix))